# Database Seeders - دليل استخدام Seeders

## 🚀 كيفية استخدام Seeders

### 1. إنشاء Super Admin فقط

```bash
cd backend
npm run seed:admin
```

هذا سينشئ:
- ✅ حساب Super Admin: `admin@esdaly.com`
- ✅ كلمة المرور: `admin123`
- ⚠️ **تأكد من تغيير كلمة المرور بعد أول تسجيل دخول!**

---

### 2. إنشاء بيانات كاملة (Admin + Categories + Sample Products)

```bash
cd backend
npm run seed
```

هذا سينشئ:
- ✅ Super Admin User
- ✅ Sample Categories (سديلات، عبايات، أغطية رأس)
- ✅ Sample Products (منتجات تجريبية)

---

## 📋 بيانات Super Admin الافتراضية

```
Email: admin@esdaly.com
Password: admin123
Role: admin
Name: Super Admin
```

---

## ⚠️ ملاحظات مهمة

1. **تغيير كلمة المرور**: بعد أول تسجيل دخول، غيّر كلمة المرور فوراً
2. **الأمان**: لا تستخدم `admin123` في Production
3. **التشغيل**: تأكد من تشغيل MongoDB قبل تشغيل Seeder
4. **التكرار**: يمكنك تشغيل Seeder عدة مرات - لن ينشئ بيانات مكررة

---

## 🔧 حل المشاكل

### "Cannot connect to MongoDB"
- تأكد من تشغيل MongoDB
- تحقق من `MONGODB_URI` في `.env`

### "Admin already exists"
- هذا طبيعي - Seeder يتحقق من وجود المستخدم أولاً
- إذا كان موجوداً، سيتم تحديثه فقط

---

## 📝 تخصيص البيانات

يمكنك تعديل:
- `backend/seeders/index.js` - لتغيير البيانات الافتراضية
- `backend/seeders/admin.seeder.js` - لتغيير بيانات Admin

---

## ✅ بعد تشغيل Seeder

1. افتح: `http://localhost:3000/admin/login`
2. سجل دخول بـ:
   - Email: `admin@esdaly.com`
   - Password: `admin123`
3. غيّر كلمة المرور فوراً!

---

**جاهز! الآن يمكنك استخدام Seeder لإنشاء Admin تلقائياً! 🎉**
